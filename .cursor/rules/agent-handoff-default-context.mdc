---
description: Use the project handoff doc when no session context exists
alwaysApply: true
---

# Default Context Source

If there is not enough session context to understand `autoresearch`, read `docs/agent-handoff.md` before making changes or answering setup, architecture, or roadmap questions.

Treat that handoff doc as the default onboarding source of truth until fresher task-specific context is available.
