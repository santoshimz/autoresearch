[build]
builder = "DOCKERFILE"

[deploy]
startCommand = "./scripts/railway_entrypoint.sh"
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
